<?php

/** NOTE @begin->devoops  */
$theme = 'devoops'.DS;
$default = 'kml'.DS;
$core = 'core'.DS;

// NOTE Bootstrap
			/** @css */
      e($this->Html->css($default.'modules/performance/main', 'stylesheet', array('inline'=>false)));
      /** @js */
      // e($this->Html->script($theme.'jquery-ui/jquery-ui.min',false));

?>
